// Write a program to calculate the area of a square

#include<stdio.h>

int main() {
    int a;
    printf("What is  length of a line of square: ");
    scanf("%d",&a);

    printf("The Area of square: %d sq",a*a);
    return 0;
}